# Brand component usage

`MzayaIcon.jsx` remains the app-facing component. The canonical SVG files live in `src/assets/brand/logos/`. Use the SVG imports for static image contexts and the component where color/size needs to remain dynamic.
