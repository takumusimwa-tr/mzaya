import {useState} from 'react';import PropTypes from 'prop-types';
export default function MessageComposer({onSend,onTyping,onTypingStop,disabled}){const[value,setValue]=useState('');const submit=async(e)=>{e.preventDefault();const body=value.trim();if(!body||disabled)return;setValue('');onTypingStop();try{await onSend({body})}catch{setValue(body)}};return <form className="message-composer" onSubmit={submit}><textarea value={value} rows="1" maxLength="5000" placeholder="Message your Mzaya" disabled={disabled} onChange={e=>{setValue(e.target.value);onTyping()}} onBlur={onTypingStop}/><button type="submit" disabled={disabled||!value.trim()}>Send</button></form>}
MessageComposer.propTypes={onSend:PropTypes.func.isRequired,onTyping:PropTypes.func.isRequired,onTypingStop:PropTypes.func.isRequired,disabled:PropTypes.bool};MessageComposer.defaultProps={disabled:false};