const router=require('express').Router();const {authenticate,requireRole}=require('../middleware/auth.middleware');const {USER_ROLE}=require('../config/constants');const {validateRequest}=require('../middleware/validateRequest');const c=require('../controllers/profitability.controller');const {dashboardQuery}=require('../validators/profitability.validator');router.use(authenticate);router.use(requireRole(USER_ROLE.ADMIN));router.get('/dashboard',validateRequest(dashboardQuery,'query'),c.dashboard);module.exports=router;
