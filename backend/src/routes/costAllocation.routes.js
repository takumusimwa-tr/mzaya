const router=require('express').Router();const {authenticate,requireRole}=require('../middleware/auth.middleware');const {USER_ROLE}=require('../config/constants');const {validateRequest}=require('../middleware/validateRequest');const c=require('../controllers/costAllocation.controller');const {allocationBody}=require('../validators/profitability.validator');router.use(authenticate);router.use(requireRole(USER_ROLE.ADMIN));router.get('/',c.list);router.post('/runs',validateRequest(allocationBody),c.run);module.exports=router;
